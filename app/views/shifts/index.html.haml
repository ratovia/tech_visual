.table
  .date-axis
    - 13.times do |num|
      - if num == 0
        .time-cell
      - else
        .time-cell
          = num + 9
          \:00
  / ユーザーの仮置き
  - 4.times do |num|
    .name-axis
      .cell
        / ユーザー名の仮置き
        user#{num + 1}
      .worktime
        - 12.times do |num|
          .worktime-cell
